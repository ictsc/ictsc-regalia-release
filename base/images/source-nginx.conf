server {
    listen 8080 default_server;
    listen [::]:8080 default_server;
    server_name _;
    root /var/lib/git-sync/current/images;

    location = /healthz {
        access_log off;
        return 200;
    }

    location / {
        try_files $uri =404;
    }
}
